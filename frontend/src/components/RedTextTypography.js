import Typography from "@mui/material/Typography";
import { withStyles } from "@mui/styles";

const RedTextTypography = withStyles({
  root: {
    color: "#e1434b",
  },
})(Typography);

export default RedTextTypography;
